# Backend – Mis Eventos (FastAPI)

## Requisitos
- Python 3.12 (para desarrollo local) o Docker
- Poetry 1.8+

## Comandos útiles (local)
```bash
poetry install
poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
